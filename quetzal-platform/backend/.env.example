# Quetzal Platform Backend - Environment Example

# App
NODE_ENV=development
PORT=3000

# Postgres (separate vars)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=quetzal_db
DB_SSL=false

# Alternatively you can use a single DATABASE_URL
# Example: postgres://postgres:your_password_here@localhost:5432/quetzal_db
# DATABASE_URL=
