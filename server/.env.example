# ============================================
# EXAMPLE ENVIRONMENT VARIABLES
# ============================================
# Copiar este archivo a .env y ajustar valores
# NO commitear .env a git

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=quetzal_db
PGUSER=postgres
PGPASSWORD=root

# Authentication
# IMPORTANTE: En producción, generar con:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=changeme-super-secret
JWT_EXPIRES_IN=86400

# CORS Configuration
# En producción cambiar a dominio específico:
# CORS_ORIGIN=https://tu-dominio.com
CORS_ORIGIN=http://localhost:3000

# Upload Configuration (opcional)
# MAX_FILE_SIZE=2097152  # 2MB en bytes
# ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,webp

# Rate Limiting (opcional)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutos
# RATE_LIMIT_MAX_REQUESTS=100

# Logging (opcional)
# LOG_LEVEL=info  # debug, info, warn, error
# LOG_FILE_PATH=./logs/app.log

# Email Configuration (Verificación de correo)
# Para desarrollo puedes usar Gmail con App Password:
# https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password-de-16-digitos
SMTP_FROM="Quetzales <noreply@quetzales.com>"

# Frontend URL (para links de verificación)
FRONTEND_URL=http://localhost:3000

# Producción alternativa (Resend - recomendado):
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=resend
# SMTP_PASS=re_tu_api_key
